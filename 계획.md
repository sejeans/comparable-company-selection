# 비상장기업 유사기업(베타풀) 자동 선정 — 실행 계획

> 배경/문제의식은 [개요.md](./개요.md) 참고. 이 문서는 그걸 처음부터 끝까지 어떻게 만들지에 대한 실행 계획.

## 0. 최종 산출물 정의

**Streamlit 웹 대시보드** 하나로 수렴한다.

- 입력: 평가대상 기업 정보(회사명 or 업종 + 재무정보 직접 입력, 평가기준일)
- 출력:
  1. 유사기업 후보 리스트 (산업분류 매치 여부 + 텍스트 유사도 점수 + 재무비율 유사도 점수)
  2. 베타풀 인터랙티브 선택 (체크박스로 후보 넣고 빼면 개별 베타 → 중앙값 베타 → WACC 실시간 갱신)
  3. 민감도 분석 화면 (베타풀 조합에 따라 WACC이 얼마나 벌어지는지 — 이 프로젝트의 핵심 어필 포인트)
  4. (확장) 산업분류만 쓴 경우 vs 재무비율까지 쓴 경우 베타 분산 비교

## 1. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Python 3.11 | 이미 venv(`comp_comp_selec/`) 존재 |
| DART 연동 | `requests` 직접 호출 또는 `dart-fss` | 구조화 API는 dart-fss가 편하지만, 사업보고서 원문 텍스트 파싱은 직접 구현 필요할 가능성 큼 |
| KRX 주가 | `pykrx` | 일별 시세, 지수(KOSPI) 수집 |
| 텍스트 임베딩 | `sentence-transformers` (한국어 특화 모델, 예: `jhgan/ko-sroberta-multitask` 또는 `BM-K/KoSimCSE-roberta`) | 사업의 내용 유사도용 |
| 저장소 | 초기엔 **Parquet 파일 + DuckDB**, 필요시 SQLite로 승격 | 별도 DB 서버 없이 로컬에서 시계열/테이블 다루기 충분 |
| 재무비율/통계 | `pandas`, `numpy`, `statsmodels` (베타 회귀) | |
| 유사도/클러스터링 | `scikit-learn` (cosine similarity, StandardScaler, 필요시 KMeans) | |
| 대시보드 | `streamlit` | |
| 스케줄링(배치 갱신) | 처음엔 수동 스크립트 실행, 나중에 `schedule`/Windows 작업 스케줄러 | 개인 프로젝트 규모라 오버엔지니어링 지양 |

## 2. 데이터 아키텍처 (지난 논의 결론)

- **비교군 풀(전체 상장기업)** = 배치로 미리 수집·가공해서 연도별 시계열로 저장 (사전 논의 결론, [개요.md](./개요.md) ①④와 일치)
- **평가대상 기업** = 온디맨드로 그 시점 데이터만 가볍게 수집 (이미 배치 DB에 있으면 재사용)
- DART에서 필요한 소스는 3가지 (사업보고서 하나로 안 됨):
  1. 기업개황 API (`company.json`) — corp_code, 업종코드
  2. 재무제표 API (`fnlttSinglAcntAll`) — 구조화된 재무비율용 데이터
  3. 공시서류원본파일 API — 사업보고서 원문에서 "II. 사업의 내용" 파싱 → 임베딩용
- KRX 일별 주가는 별도 소스(`pykrx`)

## 3. 폴더 구조 (제안)

```
comparable-company-selection/
├── 개요.md
├── 계획.md
├── .env                      # DART_API_KEY
├── data/
│   ├── raw/                  # DART 원본 응답 캐시 (연도별)
│   └── processed/            # 정제된 parquet (재무비율, 임베딩, 기업개황)
├── src/
│   ├── collectors/
│   │   ├── dart_company.py   # 기업개황/corp_code
│   │   ├── dart_financials.py# 재무제표
│   │   ├── dart_report_text.py # 사업보고서 원문 파싱
│   │   └── krx_price.py      # 주가/지수
│   ├── pipeline/
│   │   ├── build_universe.py # 배치: 전체 상장사 풀 구축/갱신
│   │   └── embed_text.py     # 사업의 내용 임베딩
│   ├── analysis/
│   │   ├── similarity.py     # 1차(산업+텍스트), 2차(재무비율) 필터
│   │   ├── beta.py           # 베타 계산 (회귀)
│   │   └── wacc.py           # WACC + 민감도 분석
│   └── app/
│       └── streamlit_app.py  # 대시보드 엔트리포인트
└── notebooks/                # 파이프라인 검증용 스크래치
```

## 4. 단계별 로드맵

### Phase 0 — 준비 (완료/진행)
- [x] DART API 키 발급 (.env)
- [ ] `dart-fss` / `pykrx` / `sentence-transformers` / `streamlit` / `duckdb` 등 requirements 확정 및 설치

### Phase 1 — DART/KRX 원천 데이터 파이프라인
- [ ] corp_code 전체 목록 수집 (DART 고유번호 파일)
- [ ] 상장기업만 필터링 (KRX 상장종목 목록과 매칭)
- [ ] 기업개황 API로 업종코드(KSIC) 수집
- [ ] 재무제표 API로 연도별 재무제표 수집 → 재무비율 계산 함수 구현 (매출성장률/영업이익률/부채비율/자산회전율/자산규모)
- [ ] 사업보고서 원문 다운로드 + "사업의 내용" 섹션 파싱기 구현
- [ ] KRX 일별 주가 + KOSPI 지수 수집 스크립트
- **검증 기준**: 임의 상장사 5곳 정도로 위 5가지 데이터가 다 정상적으로 나오는지 노트북에서 확인

### Phase 2 — 유사기업 후보 풀 배치 구축
- [ ] `build_universe.py`: 전체 상장사 대상으로 Phase 1 파이프라인 일괄 실행 → parquet/duckdb 저장 (연도 컬럼 포함)
- [ ] 사업의 내용 텍스트 → 임베딩 벡터 생성 및 저장
- **산출물**: `data/processed/universe_{year}.parquet` (기업코드, 업종, 재무비율, 임베딩벡터)

### Phase 3 — 유사도 분석 로직
- [ ] 1차 필터: 업종코드(KSIC) 일치/유사 + 사업 텍스트 임베딩 코사인 유사도 상위 N개
- [ ] 2차 필터: 재무비율 벡터 정규화 후 유클리드/코사인 거리로 재정렬
- [ ] 후보 20개 산출 로직 함수화 (`select_candidates(target_company) -> DataFrame`)
- **검증 기준**: 알려진 동종업계 기업 2~3쌍으로 실제 유사도 상위에 잡히는지 수동 확인

### Phase 4 — 베타 / WACC 계산
- [ ] 개별 기업 베타: KRX 주간수익률 vs KOSPI 주간수익률, 2~5년 회귀 (`statsmodels`)
- [ ] 베타풀 중앙값 계산
- [ ] WACC 산출 로직 (자기자본비용 CAPM + 타인자본비용 + 자본구조 가중)
- [ ] 민감도 분석: 베타풀 조합을 바꿔가며 WACC 분포 시뮬레이션 (예: 20개 중 5개 뽑는 조합 샘플링)

### Phase 5 — Streamlit 대시보드
- [ ] 화면 1: 평가대상 기업 입력 폼
- [ ] 화면 2: 후보 리스트 테이블 (유사도 점수, 선정 근거 표시)
- [ ] 화면 3: 베타풀 체크박스 선택 → 실시간 베타/WACC 갱신
- [ ] 화면 4: 민감도 분석 시각화 (조합별 WACC 분포 히스토그램/박스플롯)
- [ ] 캐싱 처리 (`st.cache_data`)로 배치 데이터 로딩 최적화

### Phase 6 — 확장 (여력 되면)
- [ ] 실제 M&A/투자 사례 비교 검증
- [ ] 산업분류만 쓴 경우 vs 재무비율까지 쓴 경우 베타 분산 비교 화면 추가

## 5. 열린 이슈 / 결정 필요 사항

- 사업보고서 원문 파싱: 회사마다 목차 구조가 조금씩 달라서 "사업의 내용" 섹션 추출 규칙이 깨질 수 있음 → 파싱 실패 케이스 로깅하고 수동 예외처리 리스트 관리 필요
- 비상장기업(평가대상)의 재무정보는 DART에 없을 수 있음(비상장이라도 사업보고서 제출 의무 있는 곳도 있고 없는 곳도 있음) → 사용자가 직접 입력하는 폼도 같이 준비
- 배치 갱신 주기: 최초엔 최근 1~2개년만 구축하고, 검증 끝나면 과거 연도로 확장하는 순서 추천 (전체 연도 한번에 긁으면 디버깅 비용 큼)
