# 비상장기업 유사기업(베타풀) 자동 선정 — 실행 계획

> 배경/문제의식은 [개요.md](./개요.md) 참고. 이 문서는 그걸 처음부터 끝까지 어떻게 만들지에 대한 실행 계획.

## 0. 최종 산출물 정의

**Streamlit 웹 대시보드** 하나로 수렴한다.

- 입력: 평가대상 기업 정보(회사명 or 업종 + 재무정보 직접 입력, 평가기준일)
- 출력:
  1. 유사기업 후보 리스트 (산업분류 매치 여부 + 텍스트 유사도 점수 + 재무비율 유사도 점수)
  2. 베타풀 인터랙티브 선택 (체크박스로 후보 넣고 빼면 개별 베타 → 중앙값 베타 → WACC 실시간 갱신)
  3. 민감도 분석 화면 (베타풀 조합에 따라 WACC이 얼마나 벌어지는지 — 이 프로젝트의 핵심 어필 포인트)
  4. (확장) 산업분류만 쓴 경우 vs 재무비율까지 쓴 경우 베타 분산 비교

## 1. 기술 스택

| 영역 | 선택 | 비고 |
|---|---|---|
| 언어 | Python 3.11 | 이미 venv(`comp_comp_selec/`) 존재 |
| DART 연동 | `OpenDartReader` (이미 venv에 설치돼 있어 채택) | 기업개황/재무제표/원문(sub_docs, document)까지 다 커버됨. 사업의 내용 텍스트는 원문 XML을 직접 파싱해야 함(아래 참고) |
| KRX 주가 | `FinanceDataReader` | `pykrx`의 티커목록/지수 조회가 최근 KRX 정책 변경으로 로그인(KRX_ID/KRX_PW) 요구하도록 막혀서 무인증으로 다 되는 FinanceDataReader로 교체. 개별종목 시세, KOSPI 지수, 전체 상장종목 리스트 모두 지원 |
| 텍스트 임베딩 | `sentence-transformers` (한국어 특화 모델, 예: `jhgan/ko-sroberta-multitask` 또는 `BM-K/KoSimCSE-roberta`) | 사업의 내용 유사도용 |
| 저장소 | 초기엔 **Parquet 파일 + DuckDB**, 필요시 SQLite로 승격 | 별도 DB 서버 없이 로컬에서 시계열/테이블 다루기 충분 |
| 재무비율/통계 | `pandas`, `numpy`, `statsmodels` (베타 회귀) | |
| 유사도/클러스터링 | `scikit-learn` (cosine similarity, StandardScaler, 필요시 KMeans) | |
| 대시보드 | `streamlit` | |
| 스케줄링(배치 갱신) | 처음엔 수동 스크립트 실행, 나중에 `schedule`/Windows 작업 스케줄러 | 개인 프로젝트 규모라 오버엔지니어링 지양 |

## 2. 데이터 아키텍처 (지난 논의 결론)

- **비교군 풀(전체 상장기업)** = 배치로 미리 수집·가공해서 연도별 시계열로 저장 (사전 논의 결론, [개요.md](./개요.md) ①④와 일치)
- **평가대상 기업** = 온디맨드로 그 시점 데이터만 가볍게 수집 (이미 배치 DB에 있으면 재사용)
- DART에서 필요한 소스는 3가지 (사업보고서 하나로 안 됨):
  1. 기업개황 API (`company.json`) — corp_code, 업종코드
  2. 재무제표 API (`fnlttSinglAcntAll`) — 구조화된 재무비율용 데이터
  3. 공시서류원본파일 API — 사업보고서 원문에서 "II. 사업의 내용" 파싱 → 임베딩용
- KRX 일별 주가는 별도 소스(`FinanceDataReader`, pykrx는 로그인 이슈로 제외)

## 3. 폴더 구조 (제안)

```
comparable-company-selection/
├── 개요.md
├── 계획.md
├── .env                      # DART_API_KEY
├── data/
│   ├── raw/                  # DART 원본 응답 캐시 (연도별)
│   └── processed/            # 정제된 parquet (재무비율, 임베딩, 기업개황)
├── src/
│   ├── collectors/
│   │   ├── dart_company.py   # 기업개황/corp_code
│   │   ├── dart_financials.py# 재무제표
│   │   ├── dart_report_text.py # 사업보고서 원문 파싱
│   │   └── krx_price.py      # 주가/지수
│   ├── pipeline/
│   │   ├── build_universe.py # 배치: 전체 상장사 풀 구축/갱신
│   │   └── embed_text.py     # 사업의 내용 임베딩
│   ├── analysis/
│   │   ├── similarity.py     # 1차(산업+텍스트), 2차(재무비율) 필터
│   │   ├── beta.py           # 베타 계산 (회귀)
│   │   └── wacc.py           # WACC + 민감도 분석
│   └── app/
│       └── streamlit_app.py  # 대시보드 엔트리포인트
└── notebooks/                # 파이프라인 검증용 스크래치
```

## 4. 단계별 로드맵

### Phase 0 — 준비 (완료)
- [x] DART API 키 발급 (.env)
- [x] requirements 확정 및 설치 (`OpenDartReader`, `FinanceDataReader`, `pandas`, `lxml`, `beautifulsoup4` 등 — `requirements.txt` 참고. 임베딩/통계/대시보드 패키지는 Phase 2~5에서 추가)

### Phase 1 — DART/KRX 원천 데이터 파이프라인 (완료)
- [x] corp_code 전체 목록 수집 — `OpenDartReader.corp_codes` (내부에서 고유번호 파일을 받아 캐싱, `docs_cache/`)
- [x] 상장기업만 필터링 — `stock_code`가 있는 법인만 (3,988개). 별도 KRX 티커 매칭 불필요
- [x] 기업개황 API로 업종코드(KSIC) 수집 — `dart_company.get_company_info()`
- [x] 재무제표 API로 연도별 재무제표 수집 → 재무비율 계산 함수 구현 (매출성장률/영업이익률/부채비율/자산회전율) — `dart_financials.py`
- [x] 사업보고서 원문 다운로드 + "사업의 내용" 섹션 파싱기 구현 — `dart_report_text.py`
- [x] KRX 일별 주가 + KOSPI 지수 수집 스크립트 — `krx_price.py` (FinanceDataReader 기반)
- **검증 기준**: ✅ 삼성전자/SK하이닉스/NAVER/카카오/LG화학 5곳으로 통과 (`notebooks/phase1_validate.py`)
- **구현 중 발견한 실무 이슈 (해결함)**:
  - 재무제표 계정명이 회사마다 다르게 표기됨 (`자산총계` vs `자산 총계`, `매출액` vs `매출`) → 공백 정규화 + 별칭 리스트로 대응
  - 정정공시(`[기재정정]사업보고서 (2023.12)`)는 `startswith`로 못 잡힘 → `contains`로 변경
  - **sub_docs()의 eleId가 원문 XML의 ATOCID와 항상 일치하지 않음** (정정공시처럼 커버 섹션이 앞에 붙으면 어긋남 — 카카오 사례에서 확인) → eleId 매칭 대신 목차 제목 텍스트를 원문 TITLE 태그와 직접 문자열 비교하는 방식으로 변경
  - DART 원문 XML은 escape 안 된 `&` 때문에 strict 파서로 안 열림 → `lxml.etree.XMLParser(recover=True)`
  - `pykrx`의 티커목록/지수 조회가 로그인 요구로 막혀서 `FinanceDataReader`로 전량 교체 (개별 종목 시세는 pykrx도 정상 동작했음)

### Phase 2 — 유사기업 후보 풀 배치 구축 (완료)
- [x] `build_universe.py`: 전체 상장사 대상으로 Phase 1 파이프라인 일괄 실행 → jsonl로 재시작 가능하게 누적, `--finalize`로 parquet 저장
- [x] 사업의 내용 텍스트 → 임베딩 벡터 생성 및 저장 (`embed_text.py`, `jhgan/ko-sroberta-multitask`, 정규화된 768차원 벡터)
- [x] 전체 2,651개 상장사(2023년 기준) 배치 실행 완료 — 재무비율 2,286개(86%), 사업의 내용+임베딩 2,256개(85%) 확보
- **산출물**: `data/processed/universe_2023.parquet` (기업코드/업종/재무비율/`business_description`/`embedding` 768차원 벡터, 2,651행)
- **구현 중 발견한 이슈**:
  - **DART corp_codes의 stock_code는 상장폐지 종목의 옛 코드도 남아있어서 필터링이 안 됨** — 처음에 `stock_code` 존재 여부만으로 걸렀더니 3,988개가 나왔는데, corp_code 순으로 앞쪽 25개를 파일럿으로 돌려보니 전부 재무데이터/사업보고서를 못 찾음 (예: 한빛네트 036720 등 이미 폐지된 종목). `FinanceDataReader`의 실시간 KRX 상장종목 목록과 교집합을 취하도록 `dart_company.get_listed_corp_codes()`를 수정 → 2,651개로 정정
  - KONEX(초기 중소기업 전용 시장, 108개)는 거래가 희박해 베타 추정에 부적합하다고 판단해 후보 풀에서 제외 (KOSPI/KOSDAQ/KOSDAQ GLOBAL만 포함)
  - 인프라펀드(맥쿼리인프라 등)처럼 일반 제조/서비스업과 다른 공시 구조를 쓰는 법인은 재무제표/사업보고서를 못 찾는 게 정상 — 실패 로그로 자연스럽게 걸러짐
  - 전체 배치 중 17개사(카카오뱅크·인바디·한전기술 등)가 `dart.fss.or.kr` 접속 SSL 오류로 일시 실패 → 재시도로 전부 복구, `build_universe.py`에 일시적 네트워크 오류 자동 재시도(최대 3회) 로직 추가

### Phase 3 — 유사도 분석 로직 (완료)
- [x] 1차 필터: 업종코드(induty_code) 일치/유사 + 사업 텍스트 임베딩 코사인 유사도 상위 N개 (`src/analysis/similarity.py`)
- [x] 2차 필터: 재무비율(매출성장률/영업이익률/부채비율/자산회전율/규모(log 자산총계)) 표준화 벡터의 유클리드 거리로 재정렬
- [x] 후보 20개 산출 로직 함수화 — `select_candidates(target: dict, universe: pd.DataFrame) -> DataFrame`
- **검증 기준**: ✅ 3쌍 모두 통과 (`notebooks/phase3_validate.py`)
  - LG화학 → 롯데케미칼 2위 (top5: 한화솔루션·롯데케미칼·DL·대한유화·롯데정밀화학)
  - 하나금융지주 → 우리금융지주 1위 (top5: 우리금융지주·신한지주·KB금융·BNK금융지주·iM금융지주)
  - 현대제철 → 한국철강 5위 (top5: 동국제강·동국씨엠·세아제강·동국산업·한국철강)
- **구현 메모**:
  - DART induty_code는 회사마다 자릿수가 3~5자리로 들쭉날쭉해서, 목표 기업 코드 전체 자릿수부터 시작해 풀이 15개 미만이면 앞자리만 남기고 점점 완화하는 방식으로 매칭 (예: 5자리 정확매칭 실패 시 4→3→2자리)
  - 재무비율 결측치는 후보 제외 대신 풀의 중앙값으로 대체 — 안 그러면 롯데케미칼처럼 지표 하나 결측인 진짜 비교대상이 통째로 빠짐

### Phase 4 — 베타 / WACC 계산 (완료)
- [x] 개별 기업 베타: KRX 주간수익률 vs KOSPI 주간수익률 회귀 (`src/analysis/beta.py`, `statsmodels`)
- [x] 베타풀 중앙값 계산
- [x] WACC 산출 로직 (자기자본비용 CAPM + 세후 타인자본비용을 자본구조로 가중, `src/analysis/wacc.py`)
- [x] 민감도 분석: 베타풀 조합별 WACC 분포 (`beta_pool_sensitivity`, 조합 수가 적으면 전수조합 그대로 계산, 많으면 랜덤 샘플링으로 근사)
- **검증 기준(end-to-end)**: `notebooks/phase4_validate.py` — LG화학을 타겟으로 Phase 3 후보풀 → 베타 계산 → WACC 민감도까지 전 과정 통과
  - Phase 3 후보 16개(업종 매칭 풀이 20개가 안 돼서 16개로 산출됨 — 정상 동작, 해당 업종분류에 진짜 동종기업이 그만큼밖에 없다는 뜻) → 유효 베타 14개(신규상장 등으로 관측치 부족한 2개는 자동 제외)
  - 14개 중 5개를 고르는 전체 조합(2,002가지) 계산 → **WACC 6.77%~8.36% (폭 1.59%p)** — 개요.md가 말한 "베타풀 구성에 따라 WACC이 벌어진다"는 핵심 가설이 실데이터로 확인됨
- **구현 메모**:
  - 주간수익률 관측치가 20주 미만(신규상장 등)이면 베타를 계산하지 않고 None 처리 (`MIN_OBSERVATIONS`)
  - WACC 자본구조 가중치는 시가총액이 아니라 **장부가 기준 부채비율**로 단순화 (재무제표에서 바로 구할 수 있는 값 사용 — 이론적으로는 시가총액 기준이 더 정확하다는 점은 알고 있음)
  - WACC 파라미터(무위험이자율 3.5%, 시장위험프리미엄 6%, 타인자본비용 5%, 법인세율 24.2%)는 현재 하드코딩된 기본값 — Phase 5에서 대시보드 입력값으로 노출 예정

### Phase 5 — Streamlit 대시보드 (완료)
- [x] 사이드바: 평가대상 기업 입력 (상장기업 선택 데모 모드 / 비상장기업 직접 입력 모드) + WACC 파라미터 + 베타 계산 기간
- [x] 화면 ①: 후보 리스트 테이블 (텍스트 유사도·재무비율 거리·업종매칭 자릿수 등 선정 근거 표시)
- [x] 화면 ②: 베타풀 체크박스 선택(`st.data_editor`) → 중앙값 베타/WACC 실시간 갱신 + 베타 막대그래프
- [x] 화면 ③: 민감도 분석 — k(몇 개 고를지) 슬라이더 → 전체 조합 WACC 히스토그램 + 최소/최대/변동폭 지표
- [x] 캐싱 처리 (`st.cache_data`)로 유니버스 로딩/임베딩/베타 계산 최적화
- **파일**: `src/app/streamlit_app.py` — 실행: `streamlit run src/app/streamlit_app.py`
- **검증**: Playwright로 헤드리스 브라우저 구동 후 두 입력 모드(데모/직접입력) 모두 스크린샷으로 확인, 콘솔 에러 없음. 데모 모드(LG화학)는 Phase 3~4 노트북 검증치와 동일한 수치(조합 2,002개, WACC 6.77%~8.36%)가 대시보드에서도 그대로 재현됨
- **구현 메모**: Streamlit selectbox는 네이티브 `<select>`가 아니라 커스텀 콤보박스라 테스트 자동화 시 클릭 후 옵션 텍스트로 골라야 함; 베타 계산 결과는 후보 조합(stock_code 튜플)이 바뀌면 캐시 키가 달라져 재계산을 유도하도록 세션 상태에 후보 키를 같이 저장

### Phase 6 — 확장 (여력 되면)
- [ ] 실제 M&A/투자 사례 비교 검증
- [ ] 산업분류만 쓴 경우 vs 재무비율까지 쓴 경우 베타 분산 비교 화면 추가

## 5. 열린 이슈 / 결정 필요 사항

- 사업보고서 원문 파싱: 목차 제목 텍스트 매칭 방식으로 5개사는 통과했지만, Phase 2에서 전체 상장사로 돌리면 "사업의 내용"이 로마숫자가 아닌 다른 표기를 쓰는 템플릿(예: 특정 금융업)이나 목차 자체가 없는 회사가 나올 수 있음 → 배치 실행 시 실패 건 로깅해서 별도로 검토
- 비상장기업(평가대상)의 재무정보는 DART에 없을 수 있음(비상장이라도 사업보고서 제출 의무 있는 곳도 있고 없는 곳도 있음) → 사용자가 직접 입력하는 폼도 같이 준비
- 배치 갱신 주기: 최초엔 최근 1~2개년만 구축하고, 검증 끝나면 과거 연도로 확장하는 순서 추천 (전체 연도 한번에 긁으면 디버깅 비용 큼)
